<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:grid="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             x:Class="GamanaDashBoardApp.Views.DataGridView">
    <VerticalStackLayout>
        <ActivityIndicator IsRunning="{Binding IsBusy}"
                         IsVisible="{Binding IsBusy}"
                         Color="Orange"
                         HorizontalOptions="Center"
                         VerticalOptions="Center" />

        <grid:SfDataGrid x:Name="dataGrid"
                        HeightRequest="300"
                        IsVisible="{Binding IsNotBusy}"
                        AutoGenerateColumnsMode="None" />
    </VerticalStackLayout>
</ContentView>

<!--<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:grid="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             x:Class="GamanaDashBoardApp.Views.DataGridView">
    <VerticalStackLayout>
        <Label Text="Market Data" 
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <ActivityIndicator IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           Color="Orange"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />

        <grid:SfDataGrid x:Name="dataGrid"
                        ItemsSource="{Binding Data}"
                        HeightRequest="300"
                        IsVisible="{Binding IsNotBusy}"
                        AutoGenerateColumnsMode="None">
            <grid:SfDataGrid.Columns>
                <grid:DataGridTextColumn MappingName="Name" HeaderText="Coin" />
                <grid:DataGridTextColumn MappingName="CurrentPrice" HeaderText="Price" />
                <grid:DataGridTextColumn MappingName="PriceChangePercentage24H" HeaderText="24H Change" />
                <grid:DataGridTextColumn MappingName="MarketCap" HeaderText="Market Cap" />
            </grid:SfDataGrid.Columns>
        </grid:SfDataGrid>

        <Button Text="Refresh" 
                Command="{Binding LoadDataCommand}"
                IsVisible="{Binding IsNotBusy}"
                HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentView>-->